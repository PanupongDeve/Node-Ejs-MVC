version: "2"
services:
  web:
    build: 
      context: .
      dockerfile: web.Dockerfile
    ports:
      - 3000:3000
    links:
      - mongodb
  mongo:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - ./mongodb/db:/data/db




 